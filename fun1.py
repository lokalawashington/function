def format_name(f_name, l_name):
    formated_f_name = f_name.title()
    formated_f_name = l_name.title()
    print(f"{formated_f_name} {formated_f_name}")
    # .title(transforms every first letter in a word into uppercase)


format_name("washingon", "lokala")
